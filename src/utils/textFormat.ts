// 문장 종결부호(. ! ?) 뒤 공백 한 칸을 줄바꿈으로 바꿔 문장 단위로 읽기 쉽게 만든다.
// 이미 문단 구분으로 들어있는 빈 줄(\n\n)은 건드리지 않고(공백 뒤 개행이면 스킵),
// "1. " 같은 번호 목록 마커(숫자+마침표)도 줄바꿈 대상에서 제외한다.
export const breakAfterSentences = (text: string): string =>
  text.replace(/(?<!\d)([.!?])\s(?!\n)/g, '$1\n');
